name: Remote SSH Command
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Execute remote SSH commands using password
        uses: appleboy/ssh-action@v1
        with:
          host: 84.46.251.167
          username: jean
          password: lion
          port: 22022
          script: |
            cd /var/www/html/advanced_boutique
            git pull origin master
            touch lion
            echo "lion" > lion


